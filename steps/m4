# vim: ft=sh
#
# http://www.linuxfromscratch.org/lfs/view/stable/chapter05/m4.html
#

lfs_trap_err

pushd $LFS/sources/untarred/m4-*

./configure --prefix=/tools

make

if [ ${RUN_TESTS_CHAPTER_5-NO} == "YES" ]; then
  lfs_log "make check"
  make check
fi

make install

popd

exit 0
