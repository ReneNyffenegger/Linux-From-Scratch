# vi: ft=sh
#
#  http://www.linuxfromscratch.org/lfs/view/stable/chapter06/psmisc.html
#

lfs_trap_err

cd /sources/untarred/psmisc-*

./configure --prefix=/usr

make
make install

mv -v /usr/bin/fuser   /bin
mv -v /usr/bin/killall /bin
